# Basic
###############################

# Set's mod key
set $mod Mod4

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

#####################################
# Basic



# Keys
#####################################

# Basic
bindsym $mod+Return exec konsole # Start a terminal
bindsym $mod+Shift+q kill # Kill focused window
bindsym $mod+f fullscreen toggle # Enter fullscreen mode for the focused container
bindsym $mod+w layout toggle splith splitv tabbed # Change container layout (tabbed tabbed and splith)
bindsym $mod+Shift+space floating toggle # Toggle tiling / floating
bindsym $mod+space focus mode_toggle # Change focus between tiling / floating windows
bindsym $mod+Shift+r exec "i3-msg restart" # Restart i3

#bindsym $mod+l exec "i3lock -i /home/simon/GIT/dotfiles/Wallpaper/SPACE/9.png" # Lock screen
#bindsym $mod+x exec "qmenu_hud"

bindsym $mod+h split h # Split in horizontal orientation
bindsym $mod+v split v # Split in vertical orientation
# Basic

# Change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
# Change focus

# Move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
# Move focused window

# Switch to workspace
bindsym $mod+semicolon workspace $ws0
bindsym $mod+plus workspace $ws1
bindsym $mod+ecaron workspace $ws2
bindsym $mod+scaron workspace $ws3
bindsym $mod+ccaron workspace $ws4
bindsym $mod+rcaron workspace $ws5
bindsym $mod+zcaron workspace $ws6
bindsym $mod+yacute workspace $ws7
bindsym $mod+aacute workspace $ws8
bindsym $mod+iacute workspace $ws9
bindsym $mod+eacute workspace $ws10
# Switch to workspace

# Move focused container to workspace
bindsym $mod+Shift+semicolon move container to workspace $ws0
bindsym $mod+Shift+plus move container to workspace $ws1
bindsym $mod+Shift+ecaron move container to workspace $ws2
bindsym $mod+Shift+scaron move container to workspace $ws3
bindsym $mod+Shift+ccaron move container to workspace $ws4
bindsym $mod+Shift+rcaron move container to workspace $ws5
bindsym $mod+Shift+zcaron move container to workspace $ws6
bindsym $mod+Shift+yacute move container to workspace $ws7
bindsym $mod+Shift+aacute move container to workspace $ws8
bindsym $mod+Shift+iacute move container to workspace $ws9
bindsym $mod+Shift+eacute move container to workspace $ws10
# Move focused container to workspace

# Start programs
bindsym $mod+d exec sh /home/simon/.config/rofi/normal.sh # Start Rofi 
bindsym $mod+s exec /home/simon/.config/rofi/power.sh # Power menu
bindsym $mod+a exec atom # Start Atom
bindsym $mod+q exec qtcreator # Start Qt Creator
bindsym $mod+k exec krusader # Start Krusader
bindsym $mod+i exec firefox # Start Browser
bindsym $mod+c exec code # VS Code
# Start programs

# Volume controls
#bindsym XF86AudioRaiseVolume exec amixer set 'Master' 5%+ #increase sound volume
#bindsym XF86AudioLowerVolume exec amixer set 'Master' 5%- #decrease sound volume
#bindsym XF86AudioMute exec amixer set 'Master' toggle # mute sound
# Volume controls

# Gaps shortcut
set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}


# Resize mode (you can also use the mouse for that)
mode "resize" {
        bindsym using resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"
# Resize

#########################################
# Keys



# Exec
#########################################
exec --no-startup-id "~/.config/i3/exec.fish" # Run exec script
exec_always --no-startup-id "~/.config/i3/exec_always.fish" # Run exec_always script
#########################################
# Exec



# Workspaces
#############################################

# Define names of workspaces
set $ws0 "0"
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
# Define names of workspaces

# Workspaces to displays
workspace 0 output HDMI-0
workspace 1 output HDMI-0
workspace 2 output HDMI-0
workspace 3 output HDMI-0
workspace 4 output HDMI-0
workspace 5 output HDMI-0
workspace 6 output DP-0
workspace 7 output DP-0
workspace 8 output DP-0
workspace 9 output DP-0
workspace 10 output DP-0
# Workspace to display

####################################################
# Workspaces



# Assign programs to workspaces
########################################################################
assign [class="ViberPC"] 0
assign [class="Spotify"] 0
assign [class="Discord"] 0
assign [class="android-messages-nativefier"] 0
assign [class="todoist-nativefier-9983d8"] 0
assign [class="Thunderbird"] 0
########################################################################
# Assign programs to workspaces



# Look and feel
################################

# Set colors
set $focused_border_color #1f7676
set $unfocused_border_color #21252b
set $unfocused_switch_to_color #3b414d
set $urgent_border_color #854044
set $indicator_color #7E8488
# Set colors

# Window colors
#                       background         border                     text                 indicator
client.focused          $inactive-bg-color $focused_border_color      $text-color          $indicator_color
client.unfocused        $inactive-bg-color $unfocused_border_color    $inactive-text-color $indicator_color
client.focused_inactive $inactive-bg-color $unfocused_switch_to_color $inactive-text-color $indicator_color
client.urgent           $urgent-bg-color   $urgent_border_color       $text-color          $indicator_color
# Window colors


default_border pixel 5

# border_radius 5

default_floating_border pixel 5

gaps inner 8
gaps outer 8

smart_gaps on

# smart_borders no_gaps # Activate smart borders (only when there are effectively no gaps)

################################
# Look and feel



# Fonts
##############################################################
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font pango:DejaVu Sans Mono 10
font pango:DejaVu Sans Mono, Terminus Bold Semi-Condensed 11
font pango:Terminus 11px
##############################################################
# Fonts



# Window rules
###############################################################

# Avoid focusing on Plasma from Plasma
no_focus [class=”plasmashell”]
no_focus [class=”latte-dock"]
no_focus [window_type="notification"]
no_focus [id="0x2a0001e"] # Volume controls
# Avoid focusing on Plasma from Plasma

# Avoid tiling popups, dropdown windows from Plasma
for_window [class="plasmashell"] floating enable no_focus
for_window [class="Plasma"] floating enable
for_window [class="krunner"] floating enable
for_window [class="Kmix"] floating enable
for_window [class="Klipper"] floating enable
for_window [class="Plasmoidviewer"] floating enable
for_window [class="latte-dock"] floating enable
for_window [class="lattedock"] floating enable 
for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Preferences"] floating enable
for_window [window_role="About"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
# Avoid tiling popups, dropdown windows from Plasma

# Other rules
for_window [class="Toolkit"] sticky
for_window [class="net-technicpack-launcher-LauncherMain"] floating enable # Make Technic Launcher float
for_window [title="Desktop - Plasma"] kill; floating enable; # Try to kill the wallpaper set by Plasma
# Other rules

############################################################
# Plasma Integration